# Cypress Image Snapshot Examples

To view the examples, run the following commands:

```bash
git clone https://github.com/palmerhq/cypress-image-snapshot.git
cd cypress-image-snapshot/examples
npm install
npm run examples
```
